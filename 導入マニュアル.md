# 健保問い合わせサポートシステム　導入マニュアル（ご利用者向け）

このマニュアルは、本システムをお使いになる**健保組合のご担当者さま**向けの手引きです。
専門知識がなくても進められるよう、順番に説明します。

> 所要時間の目安：**初回のみ 20〜40分**（AIの準備を含む）。2回目以降はダブルクリックで起動するだけです。

---

## ＜全体の流れ＞

```
① 使うAIを1つ決める（Claude / ChatGPT / Gemini）
        ↓
② そのAIの「CLI」を入れて、ご自身のアカウントでログインする ←ここが初回だけ少し手間
        ↓
③ 本システム（kenpo-support）を起動する
        ↓
④ 画面右上で使うAIを選んで、利用開始
```

---

## ① 使うAIを1つ決める

本システムは以下の3つのAIに対応しています。**すでに有料プランをご契約のもの**を選ぶのがおすすめです。

| AI | 対応する契約の例 |
|----|------------------|
| **Claude**（クロード） | Claude Pro / Max |
| **ChatGPT**（チャットジーピーティー） | ChatGPT Plus / Pro |
| **Gemini**（ジェミニ） | Google アカウント（Gemini） |

> 後から画面で切り替えられます。まずは1つで始めれば十分です。

---

## ② AIの準備（初回だけ）

選んだAIを本システムから使えるように、「**CLI（コマンドツール）**」を入れて、
**ご自身のアカウントでログイン**します。**この作業は初回の1回だけ**です。

> 💡 この手順だけ、黒い画面（コマンドプロンプト／ターミナル）を使います。
> 不安な場合は、社内の情報システム担当の方に本ページを見せてご相談ください。

### 共通の準備：Node.js を入れる

3つのAIのCLIは「Node.js」というソフトを使います。未導入の場合は先に入れてください。

- 公式サイト <https://nodejs.org/> から「LTS」版をダウンロードして、画面に従いインストール
- インストール後、Windowsの「コマンドプロンプト」を開き、`node -v` と入力して
  バージョン番号が表示されればOKです

### 選んだAIごとの導入とログイン

コマンドプロンプト（macは「ターミナル」）に、下の表のコマンドを入力します。

| AI | 1. 導入（入力するコマンド） | 2. ログイン |
|----|------------------------------|-------------|
| **Claude** | `npm install -g @anthropic-ai/claude-code` | `claude` と入力 → 画面の案内に従いClaudeアカウントでログイン |
| **ChatGPT** | `npm install -g @openai/codex` | `codex` と入力 → 画面の案内に従いChatGPTアカウントでログイン |
| **Gemini** | `npm install -g @google/gemini-cli` | `gemini` と入力 → 画面の案内に従いGoogleアカウントでログイン |

> ログインは通常、自動的にブラウザが開いて「許可」を押す形です。
> 一度ログインすれば、次回以降は不要です。
> ※ 各ツールの最新手順は公式ページもご確認ください。

---

## ③ 本システムを起動する

1. お渡しした **`kenpo-support.exe`**（Windows）を**ダブルクリック**します
   - macの場合は `kenpo-support` をダブルクリック
2. 小さな黒い画面（起動状況の表示）が出て、**自動的にブラウザで画面が開きます**
   - 開かない場合は、ブラウザのアドレス欄に `http://127.0.0.1:8765/` と入力
3. 使い終わったら、黒い画面で **Ctrl + C** を押すか、画面を閉じれば終了します

> ⚠️ Windowsで「WindowsによってPCが保護されました」と出た場合は、
> 「詳細情報」→「実行」で起動できます（社内ルールに従ってください）。

---

## ④ 使い方

1. 画面**右上の「AI」**で、②で準備したAIを選びます
   - 準備していないAIは「（未導入）」と表示され、選べません
2. **新規問い合わせ**：カテゴリ・担当者名・問い合わせ内容を入力し、
   「**AI回答草案を生成**」を押します
   → 回答文と、根拠となる参考文献（法令・条文）が表示されます
3. **AIと相談**：草案について不明点を質問できます
4. **最終回答へ反映**：相談結果をふまえ、回答を仕上げます
5. **保存／履歴**：「対応中」「完了」で保存。「対応履歴」から検索・編集できます

---

## データの保存場所

- 問い合わせ・回答・履歴・設定は、**お使いのPCの中だけ**に保存されます
  （保存先：`ユーザーのフォルダ\.kenpo-support\kenpo_support.db`）
- 外部のサーバーには保存されません

> 📌 **AIへの送信について**：回答を作る際、問い合わせの本文は選んだAI会社
> （Anthropic／OpenAI／Google）のクラウドに送信されます。被保険者の個人情報の
> 取り扱いは、貴組合の規程に従ってご利用ください。

---

## 困ったときは（トラブルシューティング）

| 症状 | 対処 |
|------|------|
| 右上でAIが「（未導入）」になっている | ②のCLI導入・ログインが完了しているか確認。完了後、本システムを再起動 |
| 「CLIが見つかりません」と出る | そのAIのCLIが未導入。②の導入コマンドを実行 |
| 「ログイン状態・利用上限を確認」と出る | 一度 `claude` / `codex` / `gemini` を起動してログインし直す。または利用上限の可能性 |
| 応答が返ってこない／遅い | 少し待つ。AI側が混雑している場合があります |
| ブラウザ画面が開かない | アドレス欄に `http://127.0.0.1:8765/` を直接入力 |

---

## ご利用上の大切なお願い

- 本システムは**お一人での通常利用**を前提としています。
  各AIの個人向けプランの規約上、**複数名での共有や常時の大量利用**は避けてください。
- 複数のご担当者で使う場合は、**お一人ずつ本システムを入れ、各自のアカウント**でご利用ください。

---

ご不明な点は、販売元までお問い合わせください。
